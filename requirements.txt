# ─────────────────────────────────────────────────────────
# CPU-версия (быстрая сборка, ~300MB)
# torch ставится без CUDA — подходит для локального теста
#
# Чтобы вернуть GPU-версию:
#   1. Замени строку torch ниже на:
#        torch>=2.2.0  (pip сам подтянет CUDA если есть nvidia driver)
#      или явно укажи индекс:
#        --extra-index-url https://download.pytorch.org/whl/cu124
#        torch==2.2.0+cu124
#   2. В Dockerfile раскомментируй nvidia/cuda base image (см. комментарий там)
#   3. В docker-compose.yml раскомментируй deploy.resources.reservations для embedding
# ─────────────────────────────────────────────────────────

pika==1.3.2
redis==4.6.0
qdrant-client==1.9.0
numpy>=1.26
pymorphy3>=2.0
async-timeout>=4.0

# CPU-only torch (~300MB вместо ~2GB с CUDA)
# Для GPU замени на: torch>=2.2.0  (или с явным CUDA индексом, см. выше)
torch>=2.2.0+cpu ; platform_machine != "aarch64"

# sentence-transformers использует torch выше
sentence-transformers>=3.0.0

# Тестирование и валидация
pytest>=7.0
pytest-cov>=4.0
fakeredis>=2.0
pandas>=2.0
